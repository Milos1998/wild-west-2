/**
 * Executes side effects (mini flows when changing bet, changing flows etc.)
 */
export class SideEffectsFlow {
    //in constructor make reactions on certain props that trigger certain generator functions
}
