export type UiStateType = {
    balance: number,
    betPerLine: number,
    fsLeft: number,
    fsWon: number,
    maxBetPerLine: number,
    maxSelectedLines: number,
    selectedLines: number,
    win: number,
}
