import { createSlice } from "@reduxjs/toolkit";

export const reelSetSlice = createSlice({
    name: "reelSetSlice",
    initialState: {},
    reducers: {},
});

export const reelSetReducer = reelSetSlice.reducer;
